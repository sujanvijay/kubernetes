apiVersion: v1
kind: Service
metadata:
  name: svc-node
spec:
  type: NodePort
  selector:
    app: sujan-deploy
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30000
